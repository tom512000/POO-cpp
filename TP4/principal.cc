#include "VecteurND.h"
#include <iostream>
using namespace std;

int main()
{
    // Création des instances de la classe VecteurND
    // à partir des 5 constructeurs.
    VecteurND vecteur1;
    VecteurND vecteur2 (10);
    VecteurND vecteur3 (10, 5);
    VecteurND vecteur4 (); // double c++
    VecteurND vecteur5 (); // copie

    // desctructeurs
    return 0;
}
